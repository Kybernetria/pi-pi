# Brownfield adaptation instruction alias

This internal instruction is a compatibility alias for the brownfield migration planner.

- inspect the existing repository first
- map current commands, scripts, handlers, prompts, and bootstrap wiring to protocol provides/projections
- prefer reuse over replacement
- keep the migration plan source-based and deterministic
- be explicit about heuristics versus guaranteed findings
