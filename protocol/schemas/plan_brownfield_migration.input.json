{
  "type": "object",
  "required": ["repoDir"],
  "properties": {
    "repoDir": {
      "type": "string",
      "description": "Path to an existing repository to inspect for a brownfield Pi Protocol migration."
    },
    "includeFileHints": {
      "type": "boolean",
      "description": "Whether to include concise file-level hints for the migration plan."
    },
    "preferCollaboration": {
      "type": "boolean",
      "description": "Optional hint that a collaborating pair should be considered first."
    },
    "includeInstructionDebug": {
      "type": "boolean",
      "description": "Whether to include lightweight internal instruction resolution details in the plan output."
    }
  }
}
